# 시스템 동작 흐름 (서버 기준)

> 최종 갱신: 2026-05-08
> 서버: NCP `49.50.133.160` (`/opt/advisor`)
> 엔드포인트: `POST /chat` (port 8001)

---

## 핵심 전제

`api_server.py`에서 `USE_ORCHESTRATOR_CHAT=false` 설정이므로, **legacy 경로** (`gemini_engine.chat()`)가 실제 동작한다.

---

## 1단계: 질문 수신 → Tier 분류

**파일**: `api_server.py` → `gemini_engine.py` → `model_routing_policy.py`

`classify_query_tier()`가 질문을 4개 Tier로 분류한다:

| Tier | 조건 | 처리 방식 | 응답 시간 |
|---|---|---|---|
| **0** | 품목+지역업체 언급, 금액/계약방식 없음 | LLM 완전 우회, API 직접 호출 | ~200ms |
| **1** | 금액만 있음 (품목 없음) | LLM bypass, 템플릿 답변 | ~300ms |
| **2** | 금액+품목 또는 복합 질문 | MCP 법령 수집 + LLM 추론 | ~80초 |
| **3** | 기관 유형별 전문 법적 판단 | 고위험, Pro 모델 사용 | ~80초 |

**분류 예시**:
- "LED 조명 부산 지역업체 있어?" → **Tier 0**
- "2억 물품 수의계약 가능해?" → **Tier 2**
- "공기업인데, 5천만원 여성기업 수의계약 가능?" → **Tier 3**

---

## 2단계: Tier별 처리

### Tier 0 — 업체 검색 Fast Track

**파일**: `gemini_engine.py` `_execute_tier_0_fast_track()`

| 순서 | 동작 | 파일/함수 |
|---|---|---|
| ① | 품목 키워드 추출 | `_extract_item_keyword()` — "LED 조명 구매할 건데..." → `"LED"` |
| ② | 모니터링 시스템 API 호출 | `company_api.search_by_product("LED")` → `localhost:8000` → 50건 |
| ③ | 후보 분류 | `classify_candidates()` — 조달등록/쇼핑몰/정책기업 분류 |
| ④ | 마크다운 표 생성 | `format_candidate_tables()` — 구매 경로별 3개 표 |
| ⑤ | 템플릿 조립 | `build_simple_company_search_answer()` |

- **LLM 호출 없음**
- 모니터링 시스템 API(`/opt/busan`, port 8000)가 실제 업체 데이터 제공
- API 내부에서 국세청 휴폐업 검증 + 정책기업 태그 매핑 수행

---

### Tier 2 — 법령 분석 + 업체 (주력 경로)

**파일**: `gemini_engine.py` 본체

#### 2-1. MCP Preflight (법령 수집 계획)

**함수**: `generate_mandatory_mcp_plan()`

- `topic_cluster_engine`이 질문 의도 감지 (11가지: 수의계약/입찰/지역/MAS/우수조달/정책기업/공동계약/우선구매/공사/용역/평가배점)
- 의도별 필요 법령 조문 목록 생성
- 예: "2억 물품 수의계약" → 지방계약법 시행령 제25조, 제26조, 제30조 등

#### 2-2. 법령 수집 실행

**함수**: `_execute_tier_2_mandatory_mcp()`

- **내부 DB 우선**: `law_db.sqlite` (889개 조문) 에서 먼저 검색
- **외부 MCP fallback**: 내부 DB에 없으면 법제처 API(`law.go.kr`) 호출
- 병렬 실행으로 14~15건 조문 동시 수집

#### 2-3. 업체 사전검색 (멀티라우트)

**함수**: `bypass_tier_1_2` 내부

- 7개 API 병렬 호출:

| # | API | 대상 |
|---|---|---|
| 1 | `search_by_product` | 품목별 조달업체 |
| 2 | `search_shopping_mall_product` | 종합쇼핑몰 등록 상품 |
| 3 | `search_policy_company` (여성) | 여성기업 |
| 4 | `search_policy_company` (장애인) | 장애인기업 |
| 5 | `search_policy_company` (사회적) | 사회적기업 |
| 6 | `search_certified_product` | 인증제품 |
| 7 | `search_innovation_product` | 혁신제품 |

- 결과를 LLM 컨텍스트에 주입

#### 2-4. LLM 추론

- **모델**: Vertex AI `gemini-2.5-flash` (서울 리전 `asia-northeast3`)
- **입력**: 시스템 프롬프트(`prompts/core.md`) + 수집된 법령 조문 + 업체 데이터
- **출력**: 법령 분석 + 구매 경로별 업체 그룹핑 답변

#### 2-5. 후처리

**함수**: `_finalize_answer()`

- 금지 문구 스캔 → 법적 결론 단정 차단
- 법령 인용 검증 (`_verify_and_annotate`)
- QA 로그 기록

---

## 3단계: 안전장치 (전 Tier 공통)

| 장치 | 내용 | 파일 |
|---|---|---|
| **Forbidden Phrase Scan** | "수의계약 가능합니다" 등 법적 결론 단정 표현 차단 | `gemini_engine.py` |
| **PII 제거** | 사업자등록번호, 대표자 연락처 필터링 | `company_policy.py` |
| **Legal Scope** | MCP 실패 시 `legal_conclusion_allowed=False` 강제 | `gemini_engine.py` |
| **LLM 생성 표 삭제** | Tier 0/1에서 LLM이 만든 허위 업체표 제거 | `gemini_engine.py` |
| **QA 로그** | tier/model/latency/answer 전부 기록 | `qa_log_YYYYMMDD.jsonl` |

---

## 4단계: 응답 반환

`api_server.py`가 JSON 응답으로 반환:

```json
{
  "answer": "(마크다운 답변)",
  "latency_ms": 181,
  "pipeline_mode": "legacy_gemini",
  "tier_resolved": 0,
  "model_used": "bypass_tier_0",
  "company_search_status": "success",
  "legal_conclusion_allowed": false,
  "production_deployment": "HOLD"
}
```

---

## 비활성 경로: Phase 11 Orchestrator

`USE_ORCHESTRATOR_CHAT=true`로 전환하면 동작하는 별도 파이프라인:

| 순서 | 단계 | 파일 |
|---|---|---|
| ① | `GeminiIntentRouter` — Gemini Flash로 의도/슬롯 JSON 추출 | `router/gemini_intent_router.py` |
| ② | `slot_repair` — 키워드 기반 슬롯 보정 | `router/slot_repair.py` |
| ③ | `DeterministicIntentValidator` — 규칙 기반 검증 | `router/deterministic_intent_validator.py` |
| ④ | `CompanyAPIAdapter` — 업체 검색 | `runtime/company_api_adapter.py` |
| ⑤ | `route_answer` — 규칙 기반 답변 생성 | `answer_builder/answer_type_router.py` |

- AI Studio API 키(`GEMINI_API_KEY`)를 사용 (legacy 경로의 Vertex AI와 별도)
- **현재 비활성** 상태

---

## 서비스 구성

| 서비스 | 포트 | systemd 유닛 | 경로 |
|---|---|---|---|
| 모니터링 시스템 API | 8000 | `busan-monitoring.service` | `/opt/busan` |
| 챗봇 API (pilot) | 8001 | `busan-advisor-pilot.service` | `/opt/advisor` |
| Streamlit 챗봇 UI | 8502 | `law-chatbot.service` | `/opt/advisor` |

---

## API 설정

| 항목 | 값 |
|---|---|
| GEMINI_API_KEY | `AIzaSyCkYyAwFns...` (AI Studio, Orchestrator용) |
| GOOGLE_APPLICATION_CREDENTIALS | `/opt/advisor/vertex-ai-key.json` (Vertex AI, legacy용) |
| GEMINI_MODEL | `gemini-2.5-flash` |
| 리전 | `asia-northeast3` (서울) |
| MODEL_ROUTING_MODE | `risk_based` |
| USE_MOCK_COMPANY_API | `false` (실제 API 호출) |
