# 대화맥락 2026-04-27

## 결정 사항
1. G-RT-1/G-RT-2: `innovation_source_missing=true`일 때 `tool_execution_status`는 `source_missing`, `result_status`는 `DEGRADED`, `pass=false`로 판정
2. `purchase_routes` 하드코딩을 `run_staging_verification.py`에서 수정 ("수의계약"→"수의계약 검토" 등)
3. 로컬 E2E aggregate가 API timeout이면 `local_strict_status=DEGRADED` (PASS 금지)
4. TC7 중 하나라도 `pass=false`이면 `tc7_grt_result=DEGRADED`
5. Chroma path는 resolved absolute path로 제출 (코드 표현식 금지)

## 변경 파일
| 파일 | 변경 내용 |
|---|---|
| `run_tc7_gemini_runtime.py` | G-RT-1 `innovation_missing` 우선 판정, `fallback_candidate_table_generated` 필드 추가 |
| `scripts/run_staging_verification.py` | `purchase_routes` mock 문자열 "검토" suffix 추가 |
| `.gitignore` | 검증 아티팩트 11건 제외 추가 |
| `app/gemini_engine.py` | deterministic fallback (503), app-relative chroma |
| `app/policies/candidate_formatter.py` | KEYWORD_MAP 용어 수정 |
| `app/warmup.py` | 하드코딩 `C:\dev` → app-relative 경로 |

## 해결한 문제
- purchase_routes에 "수의계약"이 단독 노출되던 문제 (staging mock 하드코딩)
- G-RT-1이 fallback 후보 존재 시 false PASS로 보고되던 문제
- git push 미완 / git status dirty 상태

## 남은 이슈
- Innovation Excel 미적재 → G-RT-1/G-RT-2 full PASS 불가
- `local_e2e_verify.py` API timeout (Gemini 503 반복)
- NCP E2E 미실행 (HOLD)
- Production deployment (HOLD)

## 다음 세션 TODO
1. Innovation Excel (`혁신제품목록.xlsx`)을 `data/` 에 배치 후 `ingest_innovation.py` 실행
2. TC7 재실행 → G-RT-1/G-RT-2 PASS 확인
3. `local_e2e_verify.py` timeout 문제 해결 (subprocess timeout 연장 또는 API retry 횟수 축소)
4. NCP clean clone → `ncp_rag_init.sh` → `ncp_e2e_verify.sh` 실행
5. 전체 PASS 확인 후 Production deployment 결정

## 하네스 검증 결과
- py_compile: 아래 참조
- import 검증: 아래 참조
- system_prompt.py: 변경 없음 (스킵)
- Git: `6a3432e` pushed to `main`
