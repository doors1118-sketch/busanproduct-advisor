# 대화맥락 2026-04-27

## 이번 세션에서 결정한 사항
- `low-risk company_search` 시 Gemini 추론 생성을 생략하고 결정론적(deterministic) 템플릿과 후보 표 응답만 즉시 리턴하는 `Fast-Track` 조기 탈출 로직 적용.
- 매뉴얼 전용 RAG(ChromDB)에 대한 Retrieval Quality 검증을 `run_staging_verification.py` 내부에 추가.

## 변경된 파일 목록
- `app/gemini_engine.py` (Fast-Track 로직 추가 및 `company_table_allowed` 등 메타데이터 보정)
- `scripts/run_staging_verification.py` (매뉴얼 RAG 조회 검증 추가)

## 해결한 문제 / 남은 이슈
- **해결된 문제:** 
  - `7_3A_Company_Search_Real` 및 기타 지역업체 추천 기능에서 발생하던 지연(30초 초과) 문제가 `Fast-Track` 도입으로 비약적으로 개선 (30s+ -> 1-2s).
  - 매뉴얼 RAG가 무사히 호출되며 Query("수의계약 유의사항") 기준 결과 3건이 정상 조회되고 있음을 확인(`verified`).
- **남은 이슈:** 
  - `load_manuals_to_rag.py` 및 `ingest_laws.py`를 백그라운드 프로세스로 띄워 동기화 중이나, PyMuPDF 관련 파싱 에러(일부 파일 격리) 디버깅은 여전히 필요.
  - NCP 서버(49.50.133.160) SSH 접속 복구 대기 상태.

## 다음 세션에서 해야 할 일
- 백그라운드 실행 중인 ChromaDB 인덱싱 및 메뉴얼 파싱 결과 확인 (drift 17.8h 해결 점검).
- PyMuPDF 파싱 실패 PDF 파일별 격리 디버깅
- NCP 배포 서버 복구 후 로컬 환경과 동기화하여 서비스 재개.
