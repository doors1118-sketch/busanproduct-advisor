# 부산 공공조달 AI 챗봇 대화맥락 및 작업 요약 (2026-04-30)

## 1. 현재 세션 작업 완료 내역
- **보안 강화 (CSV Injection 방어):** 
  - 프런트엔드의 `app.js` 내 CSV 생성 로직에 `sanitizeCsvCell()`을 적용하여 수식 시작 기호(`=, +, -, @`)를 안전하게 이스케이프 처리했습니다. 민감 정보 레드액션과 결합하여 보안을 한층 강화했습니다.
- **내부 파일럿 테스트 (1일간) 환경 구축:**
  - `app/api_server.py`에 Basic Auth 미들웨어를 추가하여, `PILOT_AUTH_ENABLED` 활성화 시 지정된 계정 정보로만 API(`/ui`, `/chat`, `/version` 등) 접근이 가능하도록 보호 조치했습니다.
  - NCP 원격 서버에 수정된 코드를 배포하고, `busan-advisor-pilot.service` 시스템 데몬 서비스를 구동한 뒤 HTTP 200 정상 반환 및 챗봇 추론 결과를 완벽히 검증했습니다.
- **사용자 편의성(UX) 증대:**
  - `frontend/index.html`에 **"💡 서비스 개요"** 카드 UI를 새로 도입하여, 챗봇이 다루는 데이터의 범위와 한계(법적 결론 자동 확정 불가)를 시각적으로 명확히 고지했습니다.
- **파일럿 비밀번호 설정 자동화 도구 제공:**
  - 관리자 PC에 `set_pilot_password.py` 스크립트를 작성하여, 대화창에 평문을 남기지 않고도 안전하게 파일럿 ID/PW를 설정하고 NCP 서버 설정을 갱신할 수 있는 체계를 마련했습니다.

## 2. 배포 및 시스템 상태
- **배포 모드:** `Production deployment = HOLD` (운영망 정식 배포 아님, 파일럿 테스트 모드)
- **접속 URL (사무실 접근 전용):** `http://49.50.133.160:8001/ui/`
- **로컬 보안 스크립트 대기:** `set_pilot_password.py`가 관리자 PC 바탕화면에 대기 중.

## 3. 잔여 이슈 및 Next Steps (사무실 이어서 할 작업)
- **[ ] 파일럿 계정 설정:** 사무실 복귀 후 로컬에서 `set_pilot_password.py`를 실행하여 파일럿용 계정(ID: `bsproduct` 권장)과 비밀번호를 서버에 반영해야 합니다.
- **[ ] 파일럿 가동 및 피드백 수집:** 사내 3~5인 직원에게 접속 URL과 계정 정보를 배포하고, 하루 동안 파일럿 운영 및 `INTERNAL_PILOT_USER_GUIDE_20260430.md` 양식 기반 피드백을 취합합니다.
- **[ ] 파일럿 종료 처리:** 하루 테스트 종료 후 원격 접속하여 `busan-advisor-pilot.service`를 중단하고, ACG의 8001번 포트 접근을 차단해야 합니다.
- **[ ] 정식 배포 준비:** 취합된 피드백에 따라 RAG 성능 미세조정 후, 최종 승인 시 `Production deployment` 모드를 해제하고 본격 가동을 검토합니다.
