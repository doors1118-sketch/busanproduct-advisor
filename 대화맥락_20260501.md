# 대화맥락 — 2026-05-01

## 현재 상태 (09:30 KST 기준)

### 서비스 상태
- korean-law-mcp: **active (running)** (v3.5.4, systemd 보강 완료)
- OC 키: **busanproduct1** (NCP e2e .env + 로컬 mcp_client.py 반영 완료)
- health check script: busanproduct1 반영 완료
- cron health check: **비활성** (법제처 연결 복구 전까지 유지)
- production_deployment: **HOLD**

### 미해결 이슈
- **법제처(www.law.go.kr = 27.101.207.66) TCP 차단**
  - NCP 서버(49.50.133.160) → 법제처 80/443 = Connection Timeout
  - Google/Naver/국세청 등 다른 외부 사이트는 정상
  - 법제처 Open 기 포털에서 busanproduct1 키로 IP 등록 완료
  - 반영 대기 중 또는 법제처 기술지원(02-2109-6446) 문의 필요

### 금지 사항
- 법제처 TCP 연결 확인 전까지 MCP 재시작/코드 수정 반복 금지
- 사용자 지시 없이 재검증 금지

### 재검증 순서 (사용자 지시 시)
1. curl/nc로 www.law.go.kr 80/443 연결 확인
2. MCP search_law 실제 호출 확인
3. health check 3회
4. cron 재활성화
5. NCP A/B/C/D Raw JSON 재실행

### 완료된 인프라 조치
- systemd: Restart=always, RestartSec=5, TimeoutStartSec=30, TimeoutStopSec=15, MemoryMax=512M
- health check: /usr/local/bin/mcp-health-check.sh (Accept 헤더 + busanproduct1)
- OC 키 변경: busanproduct → busanproduct1 (NCP + 로컬)

---
## P0 안정화 업데이트 (14:30 KST 이후 작업)

### 1. P0 안정화 작업 완료
- `timeout_policy.py`: `chain_` 계열 MCP 타임아웃 3초로 강제 하향 (`MCP_FAIL_TO_CACHE=true` 시)
- `gemini_engine.py`: 
  - `is_mcp_error` JSON 기반 파싱 우선 도입
  - 에러 시 `_mcp_cache` 오염 방지 로직 구축
  - MCP 부분 실패 시 `partial_mcp_with_missing` 상태 플래그 반영
  - FTC 우회 로직 추가 (사전 조회 전면 실패 시 LLM Loop 건너뛰기)
- `answer_builder_policy.py`: `_render_missing_mcp_table` 도입, 사용자 대면 미확인 근거 표 렌더링
- `api_server.py`: Pydantic 500 에러 해결을 위해 필드 `Optional` 속성 지정
- **QA 검증 완료**: A~F 시나리오 6종 전부 15초 이내 통과 (E/F 시나리오 지연 문제 해결)

### 2. 데이터 무결성 재동기화 (ChromaDB-BM25)
- 원격 서버 (`49.50.133.160`의 `/root/advisor/app`) 환경에서 `ingest_laws.py` 강제 재실행 완료.
- 법령 13건 적재 완료 및 BM25 Index 생성 (`bm25_laws_index.pkl`) 정상 동기화.

### 3. 검증 결과 문서화
- `P0_VERIFICATION_REPORT.md` 아티팩트에 실제 diff 및 API 실행 상태, 메타데이터 수치를 객관적으로 기록 (추상적/긍정적 묘사 배제 규정 준수).
