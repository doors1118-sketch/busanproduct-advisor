# 대화맥락_20260501 (2차 세션)

## 세션 정보
- 날짜: 2026-05-01 (21:35 ~ 22:06 KST)
- 모델: Gemini 3.1 Pro → Claude Opus 4.6 (세션 중 전환)

## 완료 사항

### Phase 10 — CacheBuilder MVP 로컬 구현 (PASS 확정)
- `scratch/company_cache_builder_mvp.py` 작성 완료
- `scratch/mock_company_records.json` mock fixture 분리 완료
- MOCK_MODE=true 로컬 dry-run 기준 PASS

### Phase 10 보완 1 — HMAC·보안 수정
- `hashlib.sha256` → `hmac.new(secret, payload, hashlib.sha256)` 표준 HMAC 교체
- 환경변수 기본값(mock-key, mock-secret) 제거, fail-fast 강제
- mock fixture를 별도 JSON 파일로 물리 분리

### Phase 10 보완 2 — 정합성 최종 보완 (PASS 확정)
- `canonicalize_business_no()`: 숫자 외 문자 제거 + 10자리 검증
- `make_internal_join_key()`: canonicalized value만 HMAC 입력
- SQLite schema: `internal_join_key TEXT PRIMARY KEY NOT NULL`
- `rejected_row_count` 계산 및 manifest 기록
- manifest 보안 스캔 보강: API key값, HASH_SECRET값, serviceKey/OC_KEY/token/password 키워드, 사업자번호 패턴, representative, 전화번호, 이메일 패턴
- DB 보안 스캔 보강: 동일 패턴 전수 검사
- 단위 테스트: 하이픈/공백/무관 동일 HMAC 출력 확인 (h1==h2==h3)

### Phase 11 — Live API 최소 연동 구현 (코드 PASS / 실행 BLOCKED)
- `requests.get()` 기반 live fetch 구현 완료
- `.env` 자동 로딩 로직 추가
- 응답 content key 자동 탐색 (`content`, `data`, `items` 등)
- field_presence_summary 자동 생성
- 민감값 마스킹 출력
- Mock regression 통과 확인

## 현재 상태
- **Production Deployment: HOLD**
- cache_current 미침범
- 서버 배포 미수행
- MCP chain / 챗봇 라우팅 / UI 변경 없음

## BLOCKED 사항
- Live API 실행이 `.env` 환경변수 부재로 차단됨
- 필요 변수:
  - `MONITORING_API_BASE_URL`
  - `MONITORING_API_KEY`
  - `COMPANY_HASH_SECRET`

## 변경 파일
- `scratch/company_cache_builder_mvp.py` (신규/덮어쓰기)
- `scratch/mock_company_records.json` (신규)
- `scratch/_check_env.py` (신규, 헬퍼)
- `cache/company/cache_new/company_master_cache.sqlite` (생성, mock 결과)
- `cache/company/cache_new/manifest.json` (생성, mock 결과)
- `docs/company_cache_builder_mvp_report.md` (신규)

## 다음 세션 작업 (Next Steps)
1. `.env`에 3개 환경변수 세팅 후 `MOCK_MODE=false` Live 검증 실행
2. Live 검증 PASS 시 Phase 11 최종 확정
3. 전체 batch 수집 범위 및 rate limit 검토 (별도 승인 필요)
4. policy_tags / tech_products 보조 테이블 Join ETL 설계
5. Atomic Swap 실행 검토 (cache_new → cache_current)
