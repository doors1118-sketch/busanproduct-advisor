# 대화맥락 — 2026-05-05

## 세션 개요
- **세션 ID**: 6635eeb0-b912-470c-b253-af8d8ca007ca
- **작업 시간**: 2026-05-05 오후
- **주제**: Gemini API 통합 파이프라인 고도화 및 Answer Builder 렌더링 품질 개선

---

## 완료 사항

### 1. Gemini Router 고도화
- `router_prompt.py`에 누락 슬롯 4종(`detail_item_code`, `company_id`, `company_type`, `quote_type`) 추가
- Canonical Value 허용값 맵핑 테이블 프롬프트에 주입
- `INTENT_ALIASES`, `ROUTING_DECISION_ALIASES` 정의 → 비허용 변형 안전 매핑
- 한국어 슬롯 값 → 영문 Canonical Value 정규화 (`normalize_slots()`)
- Flash → Salvage → Slot Repair → (필요 시) Retry → (필요 시) Pro Fallback 흐름 구현

### 2. API 에러 핸들링 보안 강화
- API 오류 시 PII/Trace 비노출, `api_key_invalid` / `api_call_failed` 요약 코드만 기록
- `api_key_invalid` 발생 시 Retry/Pro Fallback 즉시 차단
- 단위 테스트에서 GEMINI_API_KEY 환경변수 차단하여 외부 API 호출 원천 봉쇄

### 3. Answer Builder 렌더링 보강 (P0)
- `COMPANY_TYPE_LABELS`, `QUOTE_TYPE_LABELS`, `CONTRACT_METHOD_LABELS` 딕셔너리 추가
- `build_contract_review()`: company_type, quote_type, contract_method 요약 bullet 추가
- `build_mixed_flow()`: contract_review 인텐트 또는 관련 슬롯 존재 시 [계약 검토 요약] 섹션 강제 생성
- 결과: Scenario 2에서 100,000,000원 표시, Scenario 3에서 여성기업/1인견적 표시, Scenario 4에서 일반기업/2인이상견적 표시

### 4. E2E 리포트 판정 로직 3단계 분리
- `Slot Repair Used: YES/NO` — 실제 수리 발생 여부
- `Slot Repair Needed: YES/NO` — 수리가 필요한 상황이었는가
- `Slot Repair Recovery: NOT_NEEDED/PASS/FAIL` — 수리 결과

### 5. Scenario 6 처리 정책 전면 개편
- "조문 발췌형 Evidence" → "조건별 안내형 Evidence"로 전환
- 품목명 없음 + 수의계약 의도 시: 업체 조회 조건 확인 필요 + 수의계약 검토 기준 요약 + 추가 필요 정보
- 금액 미입력 시 threshold 기준금액 NOTE 박스 미노출
- 조문 발췌 `<details>` 태그 해당 케이스에서 출력하지 않음

### 6. 시스템 운영 개요 문서 작성
- `SYSTEM_OPERATION_OVERVIEW.md` 생성 (기존 PART1/PART2/chatgpt_direction_evaluation 종합)

---

## 최종 검증 결과
- Live Gemini E2E: 6개 시나리오 전체 **PASS** (Gemini API Status: OK)
- 단위/회귀 테스트: 전체 PASS

---

## 변경 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `app/answer_builder/answer_type_router.py` | 라벨 딕셔너리, mixed_flow 계약 요약, contract_review 보강 |
| `app/answer_builder/evidence_answer_builder.py` | Scenario 6 조건별 안내형 Evidence, router_slots 전달 |
| `app/router/gemini_intent_router.py` | API 에러 요약, api_key_invalid 차단 |
| `app/router/router_prompt.py` | 누락 슬롯 추가, Canonical Value 테이블 |
| `scratch/run_live_e2e_with_gemini.py` | Slot Repair 3단계 판정 |
| `tests/test_chatbot_runtime_orchestrator.py` | API key 환경 격리 |
| `tests/test_api_error_fallback.py` | API 에러 fallback 테스트 |
| `SYSTEM_OPERATION_OVERVIEW.md` | 시스템 운영 개요 설명 자료 (신규) |

---

## 잔여 이슈 및 Next Steps

### P0
- Source Chain Completion: 핵심 5~8개 규칙(소액수의, 지역제한, MAS 2단계 등) 수동 매핑 우선 진행
- 수치 파라미터(resolved_value) 확정: 현재 대부분 None 상태

### P1
- /chat API endpoint 구현 (Orchestrator → Web UI/API 연결)
- Gateway/Rule Engine stub 해제 → 실제 Legal DB + Rule Engine 연동
- `google.generativeai` → `google.genai` 마이그레이션 (패키지 지원 종료 대비)

### P2
- 조문 단위 색인 구축 (시행령 텍스트 조문 번호 파싱 → 정밀 자동 매핑)
- Scenario 6 재실행 후 실제 출력 검증 (코드 수정 후 API 호출 미완)
