# 대화 맥락 (2026-05-05)

## 이 세션에서 결정/작업한 사항
1. **Item Eligibility Layer (품목 적격성) P0 도입**
   - 금액(Amount Layer) 기반의 분기뿐만 아니라, `item_name` 또는 `detail_item_code` 기반의 품목 적격성 확인 로직(`item_eligibility_adapter.py`)을 구축했습니다.
   - Orchestrator의 `Intent Router`와 `Amount Layer` 사이에 품목 적격성 Resolver 단계를 명시적으로 삽입하고, `active_rule_ids`에 `R_EXPLICIT_ITEM_ELIGIBILITY`를 주입하여 런타임 결정론적 무결성을 강화했습니다.
2. **답변 출력의 안전성 및 신뢰성 보완**
   - "구매 가능합니다" 등 단정적이고 위험한 답변을 유도하지 못하도록 Answer Builder를 전면 보완했습니다 (`ambiguous` 시 "세부품명번호 확정 후 확인 필요", `data_unavailable` 등).
   - 금지어 리스트를 활용한 **Final Scan** 로직을 강화하고, 금지어 탐지 시 `[표현 제한]` 및 `degraded` 상태로 정상 전환되는 것을 방어 로직으로 확인했습니다.
3. **5가지 엣지 케이스 단위/통합 테스트 자동화**
   - Data Unavailable, Malicious Item Name, Ambiguous Candidates, Company Not Checked, Cert Verified 등 주요 예외 분기를 테스트에 추가하여(총 10/10 PASS) 검증을 고도화했습니다.

## 변경된 파일 목록
- `app/runtime/item_eligibility_adapter.py` (신규 및 P0/P1 보완)
- `app/runtime/chatbot_orchestrator.py` (Item Eligibility 단계 추가, 예외/로깅 추가)
- `app/answer_builder/evidence_answer_builder.py` (렌더링 정책 보완, 품목 적격성 출력, 금지어 Tracking 강화)
- `app/answer_builder/answer_type_router.py` (서명 변경 및 `ItemEligibilityContext` 전달 추가)
- `app/router/intent_schema.py` (`detail_item_code`, `company_id` 슬롯 추가)
- `phase8_gateway_export_flat/test_item_eligibility_v0_1.py` (통합 테스트 작성 10종)
- `phase8_gateway_export_flat/` 내부의 상응하는 파일들 동기화 수정

## 해결한 문제 / 남은 이슈
- **해결된 문제**: 단순 금액 기반 답변에 의한 오류를 원천 차단하고, 품목명/세부품명코드 기반의 "중소기업자간 경쟁제품" 여부 및 "직접생산확인 대상" 여부 판단 근거 마련. `evidence_context` 의존성 오류 및 Mock 데이터 매칭 문제를 완전 해결했습니다.
- **남은 이슈**: 현재는 Mock JSON 데이터(`item_eligibility_mock_seed_data.json`)를 사용합니다. 실 운영을 위해 DB 연동(PostgreSQL DB Repo)이 필요합니다. 또한 Company API(업체 인증 상태 API) 연동이 목업 처리되어 있습니다.

## 다음 세션에서 해야 할 일
1. 실제 API 연결: Mock API를 사용하는 `candidate_search` 단계를 실제 Company API 연결 및 에러 처리(Fail-closed) 단계로 고도화
2. Item Eligibility Layer 실제 DB 전환 작업
3. Legal Data 파이프라인 자동 업데이트 구성 (Git 등 활용)
