# 대화맥락 — 2026-05-05

## 세션 정보

- **작성일**: 2026-05-05 04:20
- **이전 세션**: ChatGPT (Phase 7~10.5 코드 리뷰 및 설계 검토)
- **현재 세션**: Antigravity (Phase 10.5 완료 + 프로젝트 정리 + 개발방향 평가)

---

## 1. 오늘 완료한 작업

### 1.1 Phase 10.5 Evidence-Based Answer Builder 보완 완료

- scan metadata 갱신 추가 (`blocked_phrases_found`, `forbidden_phrase_scan_passed`)
- 테스트 108/108 PASS
- git push 완료 (commit: `c776130`)

### 1.2 프로젝트 현황 보고서 작성

- `PROJECT_OVERVIEW_PART1.md` — 개발 개요, Phase별 진행, 법령 DB, 업체 연동, 테스트
- `PROJECT_OVERVIEW_PART2.md` — 코드 구조, 커밋 이력, 향후 계획

### 1.3 ChatGPT 대화 분석 및 개발방향 평가

- ChatGPT 대화 파일 (26,832줄, 1MB) 전체 분석
- 개발방향 적합성 평가 완료
- Source Map 오매핑 문제 발견 및 해결 방안 도출

---

## 2. 핵심 발견: Source Map 오매핑

### 문제

Phase 9.2-C에서 자동 생성한 `purchase_support_rule_source_map.json`의 27개 partial_mapped 규칙 대부분이 **키워드 유사도만으로 잘못된 법령에 매핑됨.**

### 대표 사례

R_DIRECT_GENERAL_SMALL_AMOUNT (소액수의계약):
- 매핑된 7개 source: 공정거래위원회 수의계약 지침 등 (전부 무관)
- 핵심 source인 "지방계약법 시행령 제25조"는 unmatched로 남아 있음
- 원인: 법령 제목에 "수의계약"이 없어서 키워드 매칭에서 빠짐

### 법령 DB 자체는 정상

- legal_db_v0_1_3.sqlite에 지방계약법 시행령은 들어있음
- 문제는 DB가 아니라 **자동 매핑 스크립트의 매칭 정확도**

---

## 3. 내일 (사무실) 작업: Source Map 매핑 수정

### 해결 방법: 약칭 → 정식명 매핑 테이블

```python
LAW_ALIAS_MAP = {
    "지방계약법": "지방자치단체를 당사자로 하는 계약에 관한 법률",
    "지방계약법 시행령": "지방자치단체를 당사자로 하는 계약에 관한 법률 시행령",
    "국가계약법": "국가를 당사자로 하는 계약에 관한 법률",
    "국가계약법 시행령": "국가를 당사자로 하는 계약에 관한 법률 시행령",
    "조달사업법": "조달사업에 관한 법률",
    "조달사업법 시행령": "조달사업에 관한 법률 시행령",
    ...
}
```

### 작업 순서

1. 약칭 매핑 테이블 작성 (10~15개, 30분)
2. Rule Catalog의 legal_basis에서 약칭 추출 → 정식명 변환 → legal_db에서 source_id 매칭
3. 매핑 스크립트 작성 (1~2시간)
4. 결과 검토 + `purchase_support_rule_source_map.merged.json` 생성

### 효과

- 27개 partial_mapped 중 상당수가 정확한 source로 연결
- 무관한 법령 매핑 제거
- Evidence Builder가 구체적 법령 조문 표시 가능

---

## 4. 시스템 아키텍처 정리

### 실제 사용 빈도

```
매 질문마다 사용:
  ① Gemini API     → 질문 파싱만 (의도·금액·품목 추출, 1회)
  ② 내부 법령 DB    → 법령 근거 조회
  ③ 모니터링 시스템  → 후보업체 조회

가끔 사용:
  ④ RAG (ChromaDB) → 기관 내규·매뉴얼 참조

거의 안 씀:
  ⑤ Korea-law-MCP  → 법령 업데이트 검증 시에만
```

### Gemini API 역할

- **질문 파싱에만 1회 사용** (Intent Router)
- **답변 생성에는 개입 안 함** (Answer Builder가 법령 DB + Rule Engine으로 조립)
- 환각 방지를 위한 핵심 설계

### 두 시스템 공존 상태

| | 기존 시스템 (gemini_engine.py) | 새 시스템 (Runtime Orchestrator) |
|---|---|---|
| Gemini 답변 개입 | 100% (Gemini가 답변 직접 작성) | 0% (내부 로직만) |
| 서버 배포 | ✅ 현재 운영 중 | ❌ 개발 중 |
| 환각 위험 | 있음 (금지 표현 검사로 방어) | 없음 |

→ 새 시스템 완성 후 기존 시스템 교체 예정
→ 필요 시 하이브리드 (핵심 판단은 내부 로직, 문장 다듬기만 Gemini 보조)

---

## 5. ChatGPT 설계 평가 결론

### 적합한 부분
- 법적 결론 단정 금지 원칙
- 수치 하드코딩 금지 (법령 조문에서 읽어옴)
- Fail-Closed 안전성
- Gateway read-only / Rule Engine / Answer Builder 역할 분리
- Gemini 사용 최소화 (파싱에만)

### 주의 필요
- Korea-law-MCP 완전 배제보다는 "업데이트 보조" 역할로 유지
- Source Map 자동 매핑 품질 문제 → 약칭 매핑 테이블로 해결

---

## 6. 향후 순서 (확정)

```
내일 (사무실)     이번주          다음주
    │              │              │
    ▼              ▼              ▼
Source Map     금액 분기 로직    /chat API
매핑 수정      (Rule Engine     + 실 테스트
(약칭 테이블)   stub 해제)

"확인 필요"     "시행령 제25조    실제 사용 가능
만 나옴         기준 ○○원"      상태
```

---

## 7. 금지사항 (항상 적용)

1. "계약 가능합니다", "구매 가능합니다", "수의계약 가능합니다" 등 법적 결론 단정
2. 5천만원, 1억원, 7.5점 등 법령 기준 수치 확정 출력 (source chain 완료 전)
3. expected_value_hint 사용자 답변 출력
4. legal_db write
5. source map 직접 덮어쓰기 (patch/merged 방식만 허용)
6. production 배포 (내부 테스트 완료 전)

---

## 8. 참고 파일

| 파일 | 용도 |
|------|------|
| `PROJECT_OVERVIEW_PART1.md` | 프로젝트 전체 현황 |
| `PROJECT_OVERVIEW_PART2.md` | 코드 구조 + 향후 계획 |
| `ChatGPT.md` (바탕화면) | ChatGPT 대화 원문 (26,832줄) |
| `chatgpt_direction_evaluation.md` (artifact) | 개발방향 평가 상세 |
| `purchase_support_rule_source_map.json` | 현재 source map (오매핑 포함) |
| `local_purchase_support_rule_catalog.json` | 37개 규칙 카탈로그 |
