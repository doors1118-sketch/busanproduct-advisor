# 대화맥락 — 2026-04-26 (사무실)

## 이 세션에서 결정한 사항
1. Python 3.12.10 기반 `.venv312` 가상환경에서 ChromaDB 인덱스를 네이티브 재생성 (tenant/schema 불일치 해결)
2. ChromaDB 퍼시스턴스 경로를 Laws/Manuals 분리 (`CHROMA_LAWS_DIR`, `CHROMA_MANUALS_DIR`)
3. Manuals RAG 임시 비활성화 (`RAG_MANUALS_ENABLED=false`) → rag_status=PARTIAL_DEGRADED
4. 외부 API(`shopping_mall`, `company_api`)에 HTTP socket timeout=5s 하드코딩
5. ThreadPoolExecutor `with` 블록 대신 `pool.shutdown(wait=False)` 방식으로 stall 방지
6. staging_verification_result.json에 16개 필수 필드 삽입 완료
7. Production deployment: HOLD 유지

## 변경된 파일 목록
| 파일 | 변경 내용 |
|------|----------|
| `app/gemini_engine.py` | timeout 래핑, manuals RAG toggle, ThreadPool 비차단 shutdown |
| `app/warmup.py` | laws/manuals 분리 Chroma 헬스체크 |
| `app/ingest_manuals.py` | CHROMA_MANUALS_DIR 환경변수, PyMuPDF 에러 격리 → JSON 기록 |
| `app/ingest_laws.py` | CHROMA_LAWS_DIR 환경변수 |
| `app/ingest_pps_qa.py` | CHROMA_MANUALS_DIR 환경변수 |
| `scripts/run_staging_verification.py` | 16개 필수 JSON 필드 삽입, 프롬프트 해시 추출 |
| `.gitignore` | 보안 규칙 강화 (venv, sqlite, key, credentials 등) |
| `CURRENT_STATUS.md` | 상태 리포트 전면 재작성 |
| `patch_*.py`, `run_all.ps1`, `check_chroma_db.py` | 유틸리티 신규 추가 |

## 해결한 문제
- ChromaDB "Could not connect to tenant default_tenant" → 네이티브 Python 3.12 재생성으로 해결
- Laws 808건 임베딩 적재 성공 (CPU 전용)
- staging 검증 스크립트 무한 로딩 → HTTP timeout 하드코딩 + ThreadPool 비차단 방식으로 해결
- .gitignore 보안 규칙 누락 보완

## 남은 이슈
- **manuals_chromadb_status=FAILED**: HNSW 인덱스 로딩 에러 (PyMuPDF dict parsing 오류에서 전파)
- **NCP 서버 통신 불가**: 49.50.133.160 SSH 타임아웃, 서비스 프로세스 미응답
- **BM25-ChromaDB drift 2.7h**: `python ingest_laws.py` 재동기화 필요
- **runtime_integration**: PARTIAL_PASS (manuals RAG + shopping_mall 미해결)

## 다음 세션에서 해야 할 일
1. manuals 컬렉션 독립 재생성 (CHROMA_MANUALS_DIR에 clean ingest)
2. PyMuPDF 파싱 실패 PDF 파일별 격리 디버깅
3. NCP 서버 SSH 복구 → 서비스 프로세스 재시작
4. 서버 복구 후 로컬 ChromaDB 인덱스 동기화
5. 전체 runtime_integration 재검증

## 하네스 검증 결과
- 아래 3단계에서 기록
