# 대화맥락 2026-05-07 (집 세션)

## 1. 완료된 작업

### A. 법령 데이터 파싱 고도화
- **`collect_all_law_articles.py` 조문 key 중복 수정**: 법제처 API가 `제6조`, `제6조의2`, `제6조의4`를 모두 `jo_no=6`으로 반환하여 마지막 것만 저장되던 버그 수정. 조문내용 텍스트에서 실제 조문번호(`제X조의Y`) 추출하여 고유 key 생성.
  - 결과: 공기업계약사무규칙 등 13개 법령에서 누락되었던 167개 조문 복구 (889개 → 전체 복구)

### B. 내부 도구 스코어링 개선
- **`internal_law_lookup.py` `_get_relevant_articles()` 수정**: 
  - 낱글자 매칭 → 2글자 이상 키워드 단위 매칭으로 변경
  - `text[:500]` → 전체 텍스트 검색으로 확장
  - 결과: 시행규칙 제24조가 상위 3개에 포함되도록 개선

### C. 위임 참조 자동 추적 (cross_refs)
- **DB 생성 시점에 `「법령명」 제X조` 형태의 위임 참조를 정규식으로 추출하여 `cross_refs` 필드로 저장**
  - 전체 889개 조문에서 580건의 cross_refs 추출 완료
  - `chain_law_system_internal()`이 텍스트 기반 추출 대신 저장된 cross_refs를 우선 참조
  - 공기업계약사무규칙 제6조: 4건의 cross_refs 확인됨

### D. 내부 DB 우선 조회 확장
- **`mcp_client.py`의 `get_law_text`, `get_admin_rule`에 내부 DB 우선 조회 추가**
  - MST→약칭 역매핑 활용
  - 내부 DB에 없으면 외부 MCP fallback

### E. 서버 환경 동기화
- GitHub → 서버 동기화 완료 (커밋 `6234ec2`)
- 서버 DB 재생성 완료 (167개 누락 조문 복구)
- cross_refs 580건 후처리 추가 완료
- 서비스 재시작 완료

---

## 2. 미해결 이슈

### A. 법제처 API 데이터 시점 문제
- 지방계약법 시행규칙 제24조: 서버 DB에 **100억원(구법)**으로 저장됨
- 4.24 개정(150억)이 법제처 API에 아직 미반영된 것으로 추정
- **대응**: 수동으로 해당 조문 텍스트를 최신본으로 교체 필요

### B. 업체 검색 라우팅 실패 (LED 조명 질문)
- **증상**: "LED 조명 구매할 건데, 부산 지역업체 있어?" → 빈 결과 반환
- **원인 확정**: `classify_query_tier()`에서 Tier 0이 아닌 Tier 1로 분류됨
  - Tier 0 조건: `intent_labels`에 `company_search` 등이 포함되어야 함
  - 실제: `/chat` API 호출 시 `intent_labels`가 빈 리스트이거나 매칭 안 됨
  - `company_api.search_by_product('LED')` 직접 호출 시 **50건 정상 반환** (태광조명 등)
- **company_search_status: `not_called`** — 업체 검색 자체를 호출하지 않음
- **수정 완료**: `classify_query_tier`에 키워드 fallback 추가 → Tier 0 정상 진입 확인
- **QA 로그 확인**: `tier_resolved: 0`, `model_used: bypass_tier_0`, `latency_ms: 304`
- **남은 문제**: `_execute_tier_0_fast_track()` 내부에서 업체 검색 결과 렌더링 실패
  - `company_api.search_by_product('LED')` 직접 호출 시 50건 반환 → 함수는 정상
  - Tier 0 내부의 결과 포맷팅/렌더링 경로에서 빈 결과 생성
  - `gemini_engine.py` 1207~1405번 라인 (`_execute_tier_0_fast_track`) 디버깅 필요
  - 사무실에서 만든 company_policy/company_api 연동 로직 확인 필요

---

## 3. 내부 도구 사용 전략 — 쟁점 정리

### 현재 구조 (확인된 흐름)
```
질문 입력
  ↓
classify_query_tier() → Tier 0/1/2/3 분류
  ↓
[Tier 0] _execute_tier_0_fast_track() → company_api 직접 호출 → LLM 우회
[Tier 1/2] generate_mandatory_mcp_plan()
  ├─ 1순위: topic_cluster_engine.build_preflight_plan() ← 주제×기관×계약유형 → 조문목록
  └─ 2순위: _legacy_mandatory_mcp_plan() ← 키워드 매칭 fallback
  ↓
_execute_tier_2_mandatory_mcp() → 내부DB 우선 → 외부MCP fallback
  ↓
LLM이 수집된 조문 + 업체 데이터를 읽고 답변 생성
```

### 내부 도구 11개 현황

| # | 도구 | 내부DB 우선 | 비고 |
|---|---|---|---|
| 1 | `search_law` | ✅ | 키워드 검색 |
| 2 | `get_law_text` | ✅ (이번에 추가) | MST+조문번호 직접 조회 |
| 3 | `search_interpretations` | ❌ 외부만 | 해석례 |
| 4 | `search_decisions` | ❌ 외부만 | 판례 |
| 5 | `get_annexes` | ❌ 외부만 | 별표/서식 (DB 데이터 없음) |
| 6 | `chain_law_system` | ✅ | 4단계 통합검색 |
| 7 | `chain_full_research` | ✅ 법령만 | 종합 리서치 |
| 8 | `chain_action_basis` | ❌ 외부만 | 처분 근거 |
| 9 | `verify_citations` | ❌ 외부만 | 인용 교차검증 |
| 10 | `search_admin_rule` | ✅ (이번에 추가) | 행정규칙 검색 |
| 11 | `get_admin_rule` | ✅ (이번에 추가) | 행정규칙 직접 조회 |

### 쟁점 1: 위임 참조 추적이 실제로 작동하는가?
- cross_refs 580건 DB에 저장됨
- 공기업계약사무규칙 제6조에서 4건 참조 확인
- **그러나**: 실제 chain_law_system 호출 시 참조 조문까지 LLM에 전달되는지 E2E 검증 필요
- 테스트: "공기업 지역제한 기준금액" → 제6조 → cross_refs → 국가계약법 시행규칙 제24조 자동 조회 확인

### 쟁점 2: topic_cluster_engine vs _get_relevant_articles 역할 분담
- `topic_cluster_engine`: 조문번호를 **직접 지정** (예: `제24조`)하여 정확도 높음
- `_get_relevant_articles`: 키워드 **스코어링**으로 상위 N개 조문 추출 (보완적)
- 두 경로가 각각 어떤 상황에서 작동하는지 사무실에서 논의 필요

### 쟁점 3: 외부 MCP 활용 범위
- 해석례/판례/별표는 내부 DB에 없으므로 외부 MCP 유지 불가피
- 별표(get_annexes)는 collect_all_law_articles.py에 수집 로직 추가하면 내부화 가능
- **사무실에서 논의한 "상황별 내부 도구 사용 전략" 내용 확인 필요** (집 PC에서 접근 불가)

---

## 4. 커밋 이력
- `2e2d27c` → `fix_scoring_crossref_internaldb` (스코어링+참조추적+내부DB)
- → `fix_article_key_dedup_jo_no` (조문 key 중복 수정)
- → `feat_cross_refs_graph_in_db` (cross_refs DB 추가)
- → `6234ec2` (현재 서버 HEAD)

## 5. 다음 세션 TODO
- [ ] 업체 검색 라우팅 Tier 0 진입 조건 수정 (이번 세션에서 수정 예정)
- [ ] 시행규칙 제24조 100억→150억 수동 교체
- [ ] 위임 참조 E2E 테스트 (공기업 지역제한 → 국가계약법 시행규칙 제24조)
- [ ] 사무실 대화 로그에서 "내부 도구 상황별 사용 전략" 내용 확인
- [ ] 응답 속도 최적화 (55초 → 목표 미정)
