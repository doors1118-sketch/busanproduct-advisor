# 대화맥락 — 2026.04.21

## 오늘 결정한 사항
1. **NCP 자체 호스팅**: fly.dev 대신 NCP(49.50.133.160:3000)에 Korean Law MCP 직접 배포
2. **HTTP Stateless 모드**: SSE 대신 방화벽 우회에 유리한 HTTP 모드 채택
3. **RAG 방식 채택**: 조달청 Q&A를 실시간 API 대신 ChromaDB 벡터DB로 저장
4. **적용기관별 법체계 구분**: 지방계약법/국가계약법/공기업 규칙 분리
5. **GitHub repo**: `doors1118-sketch/busanproduct-advisor`로 통합 관리

## 변경된 파일 목록
- `app/mcp_client.py` — fly.dev → NCP 전환, chain/verify 도구 추가 (9개 함수)
- `app/gemini_engine.py` — 8개 function calling + verify_citations 환각방지 + RAG 연동
- `app/system_prompt.py` — 도구 선택 규칙, 적용기관별 법체계 구분 추가
- `app/ingest_pps_qa.py` — [신규] 조달청 867건 RAG 수집/적재 스크립트
- `deploy_mcp.sh` — NCP 배포 스크립트
- `korean-law-mcp.service` — systemd 서비스 파일
- `test_mcp_tools.py` — MCP 도구 테스트 스크립트
- `test_api_key.py` — API 키 하드코딩 제거 (유출 대응)
- `.gitignore` — 참고자료 제외, 코드만 관리
- `.env` — 새 Gemini API 키로 교체

## 해결한 문제
- ✅ fly.dev 방화벽 차단 → NCP 자체 호스팅으로 해결
- ✅ 판례/해석례/별표 조회 불가 → NCP에서 전 도구 동작 확인
- ✅ ACG 포트 3000 미오픈 → 인바운드 규칙 추가
- ✅ DNS 미작동 (git clone 실패) → nameserver 8.8.8.8 추가
- ✅ Gemini API 키 유출 (GitHub push) → 새 키 발급 + 하드코딩 제거
- ✅ Gemini 2.5 Flash 503 과부하 → 2.0 Flash 폴백 테스트 성공

## 남은 이슈
- ⚠️ Gemini 2.5 Flash 일시 503 과부하 (일시적, 자동 복구 예상)
- ⚠️ NCP MCP가 nohup 아닌 systemd로 등록됨 (정상 동작 확인)

## 다음 세션에서 해야 할 일 (우선순위 순)
1. **Streamlit 챗봇 UI 테스트** — `app/🏠_홈.py`, `app/pages/💬_법령챗봇.py` 로컬 실행
2. **NCP에 Streamlit 배포** — 8501 포트 이미 ACG 오픈, git pull → pip install → streamlit run
3. **기관별 규정 RAG 추가** — 부산교통공사 등 자체 계약규정 PDF 적재
4. **임베딩 모델 개선** — 한국어 특화 임베딩으로 RAG 검색 정확도 향상

## 환경 정보
- NCP MCP: `http://49.50.133.160:3000/mcp?oc=busanproduct`
- GitHub: `https://github.com/doors1118-sketch/busanproduct-advisor`
- Gemini API 키: `.env` 파일 (AIzaSyCk...)
- 조달청 API 키: `c551b235466f...` (ingest_pps_qa.py에 저장)
- ChromaDB: `app/.chroma/` (867건 pps_qa 컬렉션)

## 검증 결과
- 구문 검증: (아래 실행 결과 참조)
- import 검증: (아래 실행 결과 참조)
