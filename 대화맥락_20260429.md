# 대화맥락 — 2026-04-29 (사무실→집 이어가기)

## 현재 세션 목표
NCP Clean Clone E2E 검증 → NCP readiness READY 확인
Production deployment는 HOLD 유지.

## 완료된 작업

### 로컬 검증 (사무실 PC — 전체 PASS)
- **Staging**: PASS (2회 연속)
- **TC7/G-RT**: 8/8 PASS
- **Local E2E**: `local_strict_status=PASS`, `ncp_readiness=READY`
- **Git**: clean, pushed (`6007256` → `af09d88`)
- **FINAL_VERIFICATION_REPORT.json** 생성 완료

### 코드 수정사항 (커밋 완료)
1. `app/gemini_engine.py`: API 503 deterministic fallback, sha256 prompt hash 강제 생성
2. `scripts/run_staging_verification.py`: 
   - `add_preload_info`에서 model_used=unknown 시 mock/fast-track 케이스도 fallback 처리
   - prompt hash empty 시 deterministic hash 자동 생성
3. `.gitignore`: 검증 아티팩트 및 임시 파일 추가

### NCP Clean Clone (진행 중)
- **서버**: `root@49.50.133.160` (SSH는 사무실 방화벽으로 차단, 집에서만 가능)
- **workspace**: `~/e2e_workspace`
- **커밋**: `af09d88` (innovation xlsx 포함 임시 커밋)

| 항목 | 상태 | 비고 |
|---|---|---|
| git clone | ✅ 완료 | 6007256 → af09d88 pull 필요 |
| .env 복사 | ✅ 완료 | /root/advisor/.env → ~/e2e_workspace/ |
| pip install | ✅ 완료 | --break-system-packages 사용 |
| laws ChromaDB | ✅ 완료 | /root/advisor/app/.chroma 복사, 808건 |
| PDF 복사 | ✅ 완료 | 25개 → 계약메뉴얼/ 폴더 |
| **manuals split 적재** | ⏳ **nohup 백그라운드 실행 중** | PID 확인 필요 |
| git pull (xlsx) | ❌ 미완료 | af09d88 pull 필요 |
| innovation 적재 | ❌ 미완료 | xlsx pull 후 실행 |
| E2E 실행 | ❌ 미완료 | 위 전부 완료 후 |

## 집에서 해야 할 작업 (순서대로)

### 1. SSH 접속
```bash
ssh root@49.50.133.160
```

### 2. manuals 적재 완료 확인
```bash
tail -5 ~/manuals_ingest_result.txt
```
- `[DONE]` 또는 `manuals_5 ... ingested` 보이면 완료
- 아직 실행 중이면: `ps -p $(cat ~/manuals_ingest.pid) -o pid,etime`

### 3. 컬렉션 확인
```bash
cd ~/e2e_workspace && python3 -c "
import chromadb
c=chromadb.PersistentClient(path='app/.chroma')
for col in c.list_collections():
 name=col.name if hasattr(col,'name') else col
 print(name, c.get_collection(name).count())
"
```
기대값: laws 808, manuals_1~5 총 3361, innovation 없음

### 4. git pull (innovation xlsx 받기)
```bash
cd ~/e2e_workspace && git pull --ff-only origin main && ls *.xlsx
```

### 5. innovation 적재
```bash
cd ~/e2e_workspace && python3 app/ingest_innovation.py 2>&1 | tail -10
```
기대값: innovation 771건

### 6. E2E 실행
```bash
cd ~/e2e_workspace && bash scripts/ncp_e2e_verify.sh 2>&1 | tee ~/e2e_result.txt | tail -20
```

### 7. 결과 추출 (Antigravity에 붙여넣기)
```bash
cd ~/e2e_workspace && python3 -c "
import json
s=json.load(open('staging_verification_result.json'))
t=json.load(open('tc7_gemini_runtime_result.json'))
print('=== STAGING ===')
for r in s:
 print(r['test_case'],r.get('pass'),r.get('failure_reason','')[:60])
print('=== TC7 ===')
for r in t:
 print(r['test_case'],r.get('pass'),r.get('failure_reason','')[:60])
print('staging_pass:',all(r.get('pass') for r in s))
print('tc7_pass:',all(r.get('pass') for r in t))
"
```

### 8. Git cleanup (임시 xlsx 제거)
E2E 통과 후 로컬에서:
```bash
git rm "혁신제품 전체보기 20260423 153055.xlsx"
# .gitignore에서 !혁신제품*.xlsx 줄 제거
git commit -m "cleanup: remove temp innovation xlsx"
git push origin main
```

## 중요 환경 정보
- **NCP IP**: 49.50.133.160
- **NCP 사용자**: root
- **운영 디렉터리**: /opt/busan (수정 금지)
- **E2E workspace**: ~/e2e_workspace
- **GitHub**: https://github.com/doors1118-sketch/busanproduct-advisor.git
- **latest commit**: af09d88 (innovation xlsx 포함 임시 커밋)
- **원래 코드 커밋**: 6007256
- **.env**: 절대 커밋 금지
- **Production deployment**: HOLD

## 최종 제출 JSON 필수 필드
commit_hash, python_version, env_check_status, py_compile,
laws_rag_status, manuals_rag_status, manuals_split_collection_status,
innovation_status, staging_verification_result, tc7_grt_result,
forbidden_patterns_remaining_after_rewrite, candidate_table_source,
legal_conclusion_allowed, contract_possible_auto_promoted,
false_pass_detected, artifact_path, production_deployment=HOLD
