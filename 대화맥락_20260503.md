# 2026-05-03 대화 맥락 (세션 종료 요약)

## 1. 현재 세션 작업 요약
- **Tier 0 Fast-Track 직접 호출 전환**: 기존 LLM `MockFunctionCall` 의존을 완전히 제거하고, `gemini_engine.py`에서 `company_api`의 검색 함수들을 직접 호출하여 Deterministic Routing을 강제했습니다.
- **JSON 응답 파싱 및 다중 속성 할당**: `candidate_policy.py`의 `classify_candidates`를 개선하여 구조화된 JSON 데이터(`candidates`, `data` 필드)를 직접 파싱하고, `candidate_types` 배열 순회를 통해 단일 업체가 여러 탭(종합쇼핑몰, 정책기업, 조달등록 등)에 동시 분류되도록 로직을 수정했습니다.
- **키워드 추출 최적화**: `_extract_item_keyword` 함수에 `수중펌프`, `펌프수문`, `안전펜스` 등 검증용 Positive Data 키워드를 추가하여, 사용자 질문에서 순수 제품명만 추출해 검색 API(`product_name` 파라미터)에 전달하도록 개선했습니다. (422 Client Error 원천 차단)
- **회귀 테스트 완수**: `scratch_test.py`를 통해 6가지 시나리오를 점검했으며, 실제 긍정 데이터를 기반으로 `server_structured_formatter`가 RAG/MCP 호출 없이도 후보표를 완벽하게 렌더링하는 것을 확인했습니다.

## 2. 발생했던 문제점 및 해결 내용
- **문제점 1**: `search_shopping_mall_product` 등 API가 빈 리스트(`[]`)를 반환하거나 422 에러 발생.
  - **해결**: 사용자 원문을 통째로 넘기던 방식에서 `_extract_item_keyword`를 경유하여 순수 핵심 키워드(예: "종합쇼핑몰 수중펌프 부산업체" -> "수중펌프")만 전달하도록 변경했습니다.
- **문제점 2**: JSON 결과를 파싱하지 못해 모든 렌더링 결과가 `none`(결과 없음)으로 떨어짐.
  - **해결**: `candidate_policy.py`에서 레거시 텍스트(string.split) 파싱 대신 `json.loads()` 기반으로 dict 객체를 순회하도록 예외 처리 및 로직을 추가했습니다.
- **문제점 3**: `search_by_product`와 `search_by_policy` 교집합 로직이 `"candidates"` 대신 `"data"` 키를 반환해 파싱 누락 발생.
  - **해결**: 교집합 결과 할당 시 키값을 `"candidates"`로 통일하고, 정책 모듈에서도 `"data"` fallback을 지원하게 만들었습니다.

## 3. 잔여 이슈 및 Next Steps
- 운영 환경 적용 및 RAG 모델 통합 검증.
- `STAGING_MODE=0` 플래그 변경 후 실 서버 DB 연동 테스트 수행.
