# 대화맥락 — 2026-05-02

## 세션 목표
- Phase 1 company_api.py Clean Rewrite 코드 검증
- deploy_chatbot.sh 5분 무한루프 문제 해결
- NCP 서버 서비스 분리 상태 진단

## 완료 사항

### 1. company_api.py 외부 제안 코드 리뷰
- 누군가 전달한 company_api.py 코드를 리뷰함
- 결론: **폐기 대상** — legacy 코드와 신규 코드가 병합(Bad Patch) 상태
  - `_api_get()` 함수 2중 정의, `requests.get()` 2회 호출, NTS/busanproduct 코드 잔존
- 현재 로컬 작업본(Clean Rewrite)은 정상 상태이므로 그대로 유지

### 2. deploy_chatbot.sh 무한루프 해결
- **원인**: 5분 cron이 무조건 `git pull` → `pip install` → `systemctl restart` 실행
  - Git merge 충돌이 발생한 상태에서 실패→재시작 무한 반복
- **수정**: `git fetch` 후 로컬/원격 해시 비교, 변경 없으면 `exit 0`, pull 실패 시 `exit 1`
- Github Push 완료 (커밋: `93db3a2`)
- NCP 서버 SSH 접속하여 `git reset --hard origin/main` 실행 → 충돌 해소, 서비스 정상화

### 3. NCP 서버 서비스 분리 진단 — 정상 확인
- **모니터링 시스템** (busan-monitor 계정)
  - `/opt/busan` 디렉터리, 700 권한
  - busan-api (포트 8000), busan-dashboard (포트 8501)
  - crontab: daily_pipeline_sync, build_api_cache, alert_check
- **챗봇 시스템** (busan-chatbot 계정)
  - `/opt/advisor` 디렉터리
  - law-chatbot (포트 8502), busan-advisor-pilot (포트 8001, 4 workers)
  - crontab: deploy_chatbot.sh (5분)
- root crontab은 비어있음 (정상 분리)
- 서버 리소스: 4Core/16GB RAM + 8GB Swap, 부하 0%

### 4. 진단 중 수정한 항목
- deploy_chatbot.sh 내부 `cd /root/advisor` → `cd /opt/advisor`로 경로 수정 (서버에서 sed 적용)
- git safe.directory에 `/opt/advisor` 추가
- `/root/advisor`와 `/opt/advisor` 양쪽 모두 최신 코드로 동기화

## 미해결 / 후속 작업
1. **`/api/chatbot/` 라우터 미구축**: 모니터링 시스템(`/opt/busan`)에 챗봇 전용 API 라우터가 아직 없음 → 별도 Phase 필요
2. **SSH 비밀번호 변경 반영**: 서버 비밀번호가 `back9900@@`로 변경됨. 기존 스크립트들(ncp_diagnostic.py 등)에 하드코딩된 구 비밀번호는 더 이상 유효하지 않음
3. **production_deployment: HOLD** 유지 중

## 시스템 상태
- 전체 5개 서비스: **ALL active**
- deploy_chatbot.sh 무한루프: **해결 완료**
- company_api.py (Clean Rewrite): **PASS**
