# 대화맥락 20260425

## 1. 이 세션에서 결정한 사항
- 부산시 조달 법령 챗봇의 "특례 기업별 수의계약 한도 혼동" 문제를 해결하기 위해 시스템 프롬프트의 판단 로직을 전면 재구조화함 (Type-Based Separation 원칙 도입).
- 모델의 추론 한계를 극복하고 더 안정적인 답변을 위해 사용 모델을 `gemini-3.1-pro`에서 `gemini-2.5-pro`로 업그레이드함.
- "Empty STOP" 현상(AI가 답변 생성을 생략하고 조기 종료하는 버그) 발생 시, 이전 단계에서 수집한 법령과 지침을 포함하여 전체 맥락을 처음부터 끝까지 완전하게 다시 작성하도록 강제 재시도 로직을 추가함.

## 2. 변경된 파일 목록
- `app/system_prompt.py`: 기업 유형(A~E) 분류 추가 및 1인/2인 견적 한도 차이 명시 규칙 추가.
- `app/gemini_engine.py`: Empty STOP 빈 응답 발생 시 재시도 프롬프트 로직 개선 및 추가.
- `.env`: GEMINI_MODEL을 `gemini-2.5-pro`로 변경.
- 테스트 및 모니터링용 파이썬 스크립트 다수 생성 (ex: `deploy_engine.py`, `check_qa_final.py` 등).

## 3. 해결한 문제 / 남은 이슈
- **해결된 문제:**
  - 여성기업 등 특례대상 한도(1억)와 일반 1인 견적 한도(2천만) 혼동 문제 해결.
  - 챗봇 UI에서 "답변 완료"만 뜨고 내용이 잘리거나 "김미향의 최종 어드바이스"만 노출되는 Empty STOP 현상 해결.
- **남은 이슈 (진행 중):**
  - Q&A 전수조사(총 249문항) 백그라운드 테스트 진행 중 (현재 50여건 완료, 약 1.5시간 내 완료 예정).

## 4. 다음 세션에서 해야 할 일
1. 전수조사 테스트 완료 결과(`qa_results_v2.json`, `qa_report_v2.md`) 분석 및 성능 평가.
2. 챗봇 속도 개선을 위한 Context Caching 도입 검토.
3. FastAPI + 프론트엔드 분리 아키텍처 중장기 계획 논의.
